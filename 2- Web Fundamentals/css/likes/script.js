function addingUp(id) {
  
  const span = document.querySelector('#' + id);
    span.textContent++;
  }


  // don't forget to call the parameter 'id'
  // don't forget to add single quotations when calling in html