/*
  Sigma
  Implement a function ​sigma(num)​ that, given a number,
  returns the sum of all positive integers from 1 up to num (inclusive).

  Ex.:
  ​sigma(3) = 6 (or 1 + 2 + 3)
  ​sigma(5)​ = 15 (or 1 + 2 + 3 + 4 + 5)
*/

/**
 *
 * @param {number} num                                          // parameter
 * @returns {number}
 */
function sigma(num) {                                           // Sigma function declared
  // your code                                                  // Set solve outside of the for loop to start at 0.
  let solve = 0                                                 // Solve is the solution to the equation. Let it start at 0 so that each number can be added to "solve" 
  for (let i = num; num >= 1; num--){                           
    solve += num;                                               // 
  }
  console.log(solve);
}
sigma(5);

/*
  Factorial
  Write a function ​factorial(num)​ that, given a number,
  returns the product (multiplication) of all positive
  integers from 1 up to num (inclusive).
  
  For example:
  ​factorial(3)​ = 6 (or 1 * 2 * 3)
  factorial(5)​ = 120 (or 1 * 2 * 3 * 4 * 5)
*/

/**
 *
 * @param {number} num
 * @returns {number}
 */
function factorial(num) {
  // your code
  let solve = 1
  for (let i = num; num >= 1; num--){

    solve *= num;
  }
  return solve;
}
const result = factorial(5);
console.log(result);
